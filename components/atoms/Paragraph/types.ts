import { IElementComponentProps } from '../../../common/interfaces';
import { TTextSize } from '../../../common/types';

export interface IProps extends IElementComponentProps {
    textSize?: TTextSize;
}